define(['../lib/util'], function( util ) {
	exports = {};
	
	exports.create = function( trigger, controller ) {
		var previousTaps = {};
		return function( frame ) {
			frame.pointables.forEach(function( finger ) {
				
			});
		}
	}
	
});